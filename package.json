{
  "name": "cloudwiki",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -r test",
    "test:server": "cd server && pnpm test",
    "test:ux": "cd ux && pnpm test",
    "test:e2e": "playwright test",
    "test:security": "pnpm audit --audit-level=high"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.1",
    "@playwright/test": "^1.59.1"
  },
  "dependencies": {
    "@milkdown/plugin-tooltip": "^7.20.0"
  }
}
